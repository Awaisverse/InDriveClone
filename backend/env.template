# Copy this file to .env and fill in your values

# Server Configuration
PORT=3000
NODE_ENV=development

# Supabase Configuration (Main Database)
# Get these from your Supabase project settings: https://app.supabase.com
SUPABASE_URL=https://jhxtkrbgkzfkklcijegt.supabase.co
# Use SERVICE_ROLE_KEY for backend (has full access) - DO NOT use anon key for backend!
SUPABASE_KEY=your-service-role-key-here
# Alternative: You can also use SUPABASE_SERVICE_ROLE_KEY instead
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
# Optional: Anon key for client-side operations (not needed for backend)
# SUPABASE_ANON_KEY=your-anon-key-here

# Redis Configuration (Cache Layer - Redis Labs)
# Your Redis endpoint: redis-14210.c10.us-east-1-2.ec2.cloud.redislabs.com:14210

# Option 1: Use connection string (RECOMMENDED for Redis Labs)
# Format: redis://username:password@host:port
# REDIS_URL=redis://default:your-redis-password@redis-14210.c10.us-east-1-2.ec2.cloud.redislabs.com:14210

# Option 2: Use individual variables (if connection string doesn't work)
REDIS_HOST=redis-14210.c10.us-east-1-2.ec2.cloud.redislabs.com
REDIS_PORT=14210
REDIS_USERNAME=default
REDIS_PASSWORD=your-redis-password-here
# Get password from Redis Labs dashboard: https://app.redislabs.com

# Note: If you get "WRONGPASS" error, try:
# 1. Remove REDIS_USERNAME (some Redis Labs setups don't need username)
# 2. Use REDIS_URL format instead
# 3. Verify password is correct in Redis Labs dashboard

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:19006

# API Configuration
API_VERSION=v1




